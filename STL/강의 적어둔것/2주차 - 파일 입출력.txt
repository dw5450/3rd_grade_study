	
////////////////////////////////////////////////////
//정수 1000개를 binary 로 저장
//////////////////////////////////////////////////////////

////////////////////////////////////////////////////
//정수 1000개를 만들어 파일에 기록하라(4000byte를 기록하자)


//다음 시간 - 정수 말고 파일에 있는 Dog를 읽어와서 정렬해보자
//숙제 - 책 2장에 어떤 내용이 있는 지 읽어보자
//////////////////////////////////////////////////////////

#include <iostream>
#include <random>			
#include <fstream>						//파일을 입축력 하기 위한 선언
#include <Windows.h>
using namespace std;

//파일 정수  "정수 1000개 메모리 그대로 저장" 에 있는 정수 1000개를
//읽어(read) 정렬한 후 화면에 출력하라
int main()
{

	//파일을 쓰는법
	//int * n = new int[1000];

	//uniform_int_distribution<int> uid;
	//default_random_engine dre;
	//for (int i = 0; i < 1000; i++)
	//	n[i] = uid(dre);

	////파일을 입력하는 
	//ofstream out ("정수 1000개 메모리 그대로 저장", ios::binary);

	////파일을 쓰는법
	////ofstream.write(데이터, 저장하고자 하는 량)
	//out.write((const char *)n, sizeof(int) * 1000);

	//delete[] n;
	

	//파일을 읽는법
	//
	//int * p = new int[1000];
	//ifstream in("정수 1000개 메모리 그대로 저장", ios::binary);

	////파일을 읽는 법
	////in.read(data, data의 크기)
	//in.read((char *)p, sizeof(int) * 1000);
	//
	//qsort(p, 1000, sizeof(int), [](const void * a, const void * b) {
	//	return *(int *)a - *(int *)b;
	//});

	//for (int i = 0; i < 20; ++i)
	//	cout << p[i] << endl;

	// delete []p

}